{% assign min = include.min | default = 1 %}
{% assign max = include.max | default = 100 %}
{% assign diff = max | minus: min %}
{% assign randomNumber = "now" | date: "%N" | modulo: diff | plus: min %}
{{ randomNumber }}
