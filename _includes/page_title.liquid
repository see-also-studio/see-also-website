{%- if page.layout == "home" -%}
{{ site.title }}{% if site.tagline %} {{ site.seperator | default: "—" }} {{ site.tagline }}{% endif %}
{%- else -%}
{{ page.title }} {{ site.seperator | default: "—" }} {{ site.title }}
{%- endif -%}
