{%- if page.layout == "home" -%}
{{ site.title }}{% if site.tagline %} {{ site.seperator | default: "—" }} {{ site.tagline }}{% endif %}
{%- elsif page.collection == 'projects' -%}
{{ site.title }} {{ site.seperator | default: "—" }} Work
{%- else -%}
{{ site.title }} {{ site.seperator | default: "—" }} {{ page.title }}
{%- endif -%}
