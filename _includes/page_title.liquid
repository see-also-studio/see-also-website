{%- if page.layout == "home" -%}
{{ site.title }}{% if site.tagline %} {{ site.seperator | default: "—" }} {{ site.tagline }}{% endif %}
{%- elsif page.collection == 'projects' -%}
Work {{ site.seperator | default: "—" }} {{ site.title }} Studio
{%- else -%}
{{ page.title }} {{ site.seperator | default: "—" }} {{ site.title }} Studio
{%- endif -%}
